# Server
PORT=5000
CLIENT_URL=http://localhost:5173
# NODE_ENV=production   # set on the deployed server

# Database (PostgreSQL)
DB_NAME=hireflow
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Auth
JWT_SECRET=change_this_to_a_long_random_string

# AI (Anthropic Claude)
ANTHROPIC_API_KEY=your_anthropic_key_here
AI_MODEL=claude-sonnet-4-6
